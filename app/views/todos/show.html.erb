<div class = "text-center"
    <ul class="list-group list-group-flush">
        <li class="list-group-item"><%= @todo.title %></li>
        <li class="list-group-item"><%= @todo.description %></li>

        <%= link_to "Done", user_todo_path, method: :delete, class: 'btn btn-primary' %>
        <%= link_to "Edit", edit_user_todo_path(current_user, @todo), class: 'btn btn-primary' %>
        <%= link_to "Back", user_todos_path(current_user, @todo), class: 'btn btn-primary' %>
    <ul>
</div>